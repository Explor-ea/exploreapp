import 'package:flutter/material.dart';

class PermissionsAgreement extends StatelessWidget {
  const PermissionsAgreement({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Padding(
      padding: const EdgeInsets.all(32.0),
      child: Column(),
    ));
  }
}
